import { Button, PageHero, Reveal, SectionHeading } from '../components/Shared'
import { caseStudies } from '../data/site'
export default function WorkPage(){return <><PageHero eyebrow="Selected work" title="Projects presented with honest status, scope and evidence." text="No invented customers, testimonials or performance results. Each case study distinguishes a live system, working demo, prototype or service capability."><Button to="/contact">Discuss a Similar Project</Button></PageHero><section className="section-pad"><div className="container-shell"><SectionHeading eyebrow="Portfolio structure" title="Show the business problem before showing the interface." text="Screenshots and verified outcomes can be added when approved for public use."/><div className="mt-12 grid gap-6 md:grid-cols-2">{caseStudies.map(({icon:Icon,category,title,status,problem,modules},i)=><Reveal key={title} delay={i*.05}><article className="card h-full overflow-hidden"><div className="bg-ink p-7 text-white dark-grid"><div className="flex items-center justify-between"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-cyan/15 text-cyan"><Icon/></div><span className="rounded-full border border-white/10 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-slate-300">{status}</span></div><p className="mt-7 text-xs font-bold uppercase tracking-[.18em] text-cyan">{category}</p><h2 className="mt-2 font-display text-2xl font-bold">{title}</h2></div><div className="p-7"><p className="text-sm leading-7 text-slate-600">{problem}</p><div className="mt-6 flex flex-wrap gap-2">{modules.map(x=><span key={x} className="rounded-lg bg-paper px-3 py-2 text-xs font-semibold text-slate-600">{x}</span>)}</div></div></article></Reveal>)}</div></div></section></>}
