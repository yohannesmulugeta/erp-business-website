import { Button, PageHero, Reveal, SectionHeading } from '../components/Shared'
import { industries } from '../data/site'
export default function IndustriesPage(){return <><PageHero eyebrow="Industries" title="Industry knowledge turns software into a practical operating tool." text="We focus on sectors where workflows, documents, stock movements and reporting requirements can be understood in detail."><Button to="/contact">Discuss Your Industry</Button></PageHero><section className="section-pad"><div className="container-shell"><SectionHeading eyebrow="Sector focus" title="Start with the operational problem inside your industry." text="Each engagement should identify the real users, records, approvals and decisions—not simply apply an industry label."/><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{industries.map(({icon:Icon,title,text},i)=><Reveal key={title} delay={i*.04}><div className="card h-full p-6"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-ink text-cyan"><Icon/></div><h2 className="mt-6 font-display text-lg font-bold">{title}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{text}</p></div></Reveal>)}</div></div></section></>}
