import { AlertTriangle, CheckCircle2 } from 'lucide-react'
import { Button, PageHero, Reveal, SectionHeading } from '../components/Shared'
import { processSteps } from '../data/site'
export default function ProcessPage(){return <><PageHero eyebrow="Delivery process" title="A controlled implementation process reduces ERP risk." text="Each phase should create clear decisions, approved outputs and documented responsibilities before the next stage begins."><Button to="/contact">Start With Discovery</Button></PageHero><section className="section-pad"><div className="container-shell"><SectionHeading eyebrow="Seven stages" title="From workflow assessment to post-launch improvement." text="The exact timeline is agreed after scope, data quality and stakeholder availability are understood."/><div className="mt-12 space-y-5">{processSteps.map((step,i)=><Reveal key={step.number} delay={i*.03}><div className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 md:grid-cols-[100px_240px_1fr] md:items-center"><span className="font-display text-3xl font-extrabold text-cyan">{step.number}</span><h2 className="font-display text-xl font-bold text-slate-950">{step.title}</h2><p className="text-sm leading-7 text-slate-600">{step.text}</p></div></Reveal>)}</div></div></section><section className="section-pad bg-white"><div className="container-shell grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-success/20 bg-success/5 p-7"><CheckCircle2 className="text-success"/><h3 className="mt-5 font-display text-xl font-bold">Version-one focus</h3><p className="mt-3 text-sm leading-7 text-slate-600">Deliver the essential workflow, stable data rules, usable reports, training and documented support before adding optional features.</p></div><div className="rounded-2xl border border-amber-300/30 bg-amber-50 p-7"><AlertTriangle className="text-amber-600"/><h3 className="mt-5 font-display text-xl font-bold">Avoid uncontrolled scope</h3><p className="mt-3 text-sm leading-7 text-slate-600">New requirements should be assessed for their effect on existing calculations, data, permissions, testing and delivery commitments.</p></div></div></section></>}
