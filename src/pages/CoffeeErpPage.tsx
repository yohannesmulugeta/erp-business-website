import { CheckCircle2, Coffee, FileText, PackageCheck, ShieldCheck, Warehouse } from 'lucide-react'
import { Button, FeatureList, PageHero, Reveal, SectionHeading } from '../components/Shared'
import { coffeeFlow } from '../data/site'

const modules=[['Purchase registration',['Supplier and source records','Coffee type and quantity','Purchase references','Supporting attachments']],['Warehouse control',['Warehouse receipts','Stock balances','Movement history','Adjustments and accountability']],['Processing & samples',['Processing logs','Input and output records','Sample tracking','Operational traceability']],['Export operations',['Export contracts','Bag and material records','Document preparation','Shipment-related reporting']],['Reports & controls',['Management dashboards','Stock reports','Activity logs','User roles and permissions']],['Documents',['Template-based outputs','PDF and Excel exports','Document history','Future Word-document workflows']]]
const icons=[Coffee,Warehouse,PackageCheck,ShieldCheck,FileText,FileText]
export default function CoffeeErpPage(){return <><PageHero eyebrow="Flagship solution" title="A specialised ERP structure for Ethiopian coffee-export operations." text="Connect purchasing, warehouse stock, processing, samples, contracts, export documents and management reporting in one controlled workflow."><Button to="/contact">Request a Coffee ERP Demo</Button></PageHero><section className="section-pad bg-white"><div className="container-shell"><SectionHeading eyebrow="Connected workflow" title="One operational record from supplier to report." text="The final module scope should be confirmed against the exporter’s actual procedures, forms, approvals and responsibilities."/><div className="mt-12 grid gap-3 sm:grid-cols-2 lg:grid-cols-7">{coffeeFlow.map((step,i)=><Reveal key={step} delay={i*.04}><div className="h-full rounded-2xl border border-slate-200 bg-paper p-4"><span className="font-display text-sm font-extrabold text-cyan">{String(i+1).padStart(2,'0')}</span><p className="mt-5 font-display text-sm font-bold text-slate-950">{step}</p></div></Reveal>)}</div></div></section><section className="section-pad"><div className="container-shell"><SectionHeading eyebrow="Potential modules" title="The system can be structured around six operational areas." text="Only approved and implemented functions should be shown as completed in the final public website."/><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{modules.map(([title,items],i)=>{const Icon=icons[i];return <Reveal key={title as string} delay={i*.04}><div className="card h-full p-6"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-ink text-cyan"><Icon/></div><h3 className="mt-5 font-display text-xl font-bold">{title}</h3><div className="mt-5"><FeatureList items={items as string[]}/></div></div></Reveal>})}</div></div></section><section className="section-pad bg-ink text-white dark-grid"><div className="container-shell grid gap-10 lg:grid-cols-2 lg:items-center"><div><SectionHeading eyebrow="Implementation principle" title="The ERP should follow verified coffee-export rules, not assumptions." text="Before implementation, each document, calculation, stock effect and approval path must be reviewed with the operating team." dark/><div className="mt-8"><Button to="/process">See Our Process</Button></div></div><div className="rounded-[28px] border border-white/10 bg-white/[.05] p-7"><h3 className="font-display text-xl font-bold">Important validation areas</h3><ul className="mt-6 space-y-4">{['Units and conversion rules','Warehouse and processing stock effects','Document fields and layouts','User roles and approval authority','Corrections, invalidation and audit history','Year-end and reporting rules'].map(x=><li key={x} className="flex gap-3 text-sm text-slate-300"><CheckCircle2 className="text-success" size={18}/>{x}</li>)}</ul></div></div></section></>}
